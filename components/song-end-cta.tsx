/**
 * End-of-song install call-to-action. Only mobile web renders a card (see the
 * `.web.tsx` sibling); on native the app is already installed, so this is a
 * no-op.
 */
export function SongEndCta(): null {
  return null;
}
