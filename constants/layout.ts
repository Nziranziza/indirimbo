/** Base spacing added below scroll content for visual breathing room */
export const CONTENT_BOTTOM_SPACING = 16;

/** Height of floating action buttons (FloatingShareButton, BackToTopButton) */
export const FAB_SIZE = 56;

/** Diameter of the round buttons in the song navigation bar (prev, audio, next) */
export const SONG_NAV_BUTTON_SIZE = 48;

/** Total clearance needed for a FAB: button height + spacing above it */
export const FAB_CLEARANCE = FAB_SIZE + CONTENT_BOTTOM_SPACING;

/** Min viewport width (web) at which the 3-column desktop frame is shown */
export const WIDE_SCREEN_BREAKPOINT = 1024;

/** Width of the left icon-only sidebar in the wide web layout */
export const SIDEBAR_WIDTH = 76;

/** Width of the right column (download card + footer) in the wide web layout */
export const RIGHT_COLUMN_WIDTH = 330;

/** Max width of the center content column (matches the mobile layout width) */
export const CONTENT_MAX_WIDTH = 428;
