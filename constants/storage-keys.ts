// Dependency-free keys safe to import from any context, including the
// static-export HTML root which can't pull in React Native modules.
export const LANGUAGE_PREFERENCE_KEY = '@indirimbo:language_preference';
